{
  "name": "the-tavern",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server",
    "setup:client": "cd the-tavern.client && npm i",
    "setup:server": "cd the-tavern.server && npm i",
    "start": "node the-tavern.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  }
}
